""" Builds a json dataset from a reviewer archive folder """

import json
import os
import argparse



if __name__ == "__main__":
	# implement method that takes in a dir of reviewer papers and creates a json file of records	
